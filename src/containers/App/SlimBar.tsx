import React from 'react'
import { SwLayoutOptions } from 'TYPES/Widget'

interface SlimBarProps {
  options?: SwLayoutOptions
}

const SlimBar: React.FC<SlimBarProps> = () => {
  return <div>???</div>
}

export default SlimBar
